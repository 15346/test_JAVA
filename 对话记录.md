# 项目开发对话记录

> 主题：从零搭建一个**极简、便于学习的前后端分离 Spring Boot 项目**（待办清单 Todo CRUD）
> 日期：2026-07-10
> 目录：`C:\Users\爱速智\Projects\test`

---

## 项目概览

| 项 | 内容 |
|---|---|
| 项目 | 待办清单（Todo），覆盖增删改查 CRUD |
| 后端 | Spring Boot 4.1.0 + Spring Data JPA + 数据库 |
| 前端 | 原生 HTML / CSS / JavaScript（用 `fetch` 调接口） |
| 数据库 | 默认 H2 内存库；可切换 MySQL |
| 运行端口 | 后端 `8081`、前端 `8000`（见下文「8080 被占用」） |
| Java | 21 |

---

## 1. 创建项目

### 1.1 需求

> 用户：在当前目录创建一个非常非常简易、便于学习的前后端分离的 springboot 项目

设计为「待办事项 Todo」增删改查——最经典的入门案例，覆盖 GET / POST / PUT / DELETE 全部接口动词。

### 1.2 环境检查

- 目录：空目录
- `java -version` → Java 21（满足 Spring Boot 4 最低 Java 17 要求）
- `mvn -version` → **未安装 Maven**
- `curl` / `unzip` / 网络（start.spring.io 可达）→ 均可用

**关键决策**：因本机无 Maven，从 Spring Initializr 拉脚手架（自带 Maven Wrapper，首次运行自动下载 Maven，免安装）：

```bash
curl -o /tmp/demo.zip -G https://start.spring.io/starter.zip \
  --data-urlencode "type=maven-project" \
  --data-urlencode "language=java" \
  --data-urlencode "baseDir=." \
  --data-urlencode "groupId=com.example" \
  --data-urlencode "artifactId=demo" \
  --data-urlencode "packageName=com.example.demo" \
  --data-urlencode "javaVersion=21" \
  --data-urlencode "dependencies=web,data-jpa,h2"
unzip -oq /tmp/demo.zip
```

脚手架为 **Spring Boot 4.1.0**，依赖名采用 4.x 新命名（`spring-boot-starter-webmvc`、`spring-boot-h2console` 等）；Maven Wrapper 为 `3.3.4` 的 `only-script` 模式，首次运行自举下载 Maven。

### 1.3 项目结构

```
test/
├── pom.xml                              # Maven 依赖
├── mvnw / mvnw.cmd                      # Maven 包装器（免安装 Maven）
├── src/main/java/com/example/demo/
│   ├── DemoApplication.java             # 启动入口
│   ├── entity/Todo.java                 # 实体（对应 todo 表）
│   ├── repository/TodoRepository.java   # 数据访问层
│   ├── service/TodoService.java         # 业务逻辑层
│   ├── controller/TodoController.java   # 接口层
│   └── config/CorsConfig.java           # 跨域配置
├── src/main/resources/
│   ├── application.properties           # 配置文件
│   └── data.sql                         # 初始测试数据
└── frontend/                            # 前端（与后端分离）
    ├── index.html
    ├── app.js
    └── style.css
```

**核心思想**：一个请求走完后端是 `Controller → Service → Repository → 数据库` 四层。

### 1.4 关键代码

接口一览（[TodoController.java](src/main/java/com/example/demo/controller/TodoController.java)）：

| 方法 | 地址 | 作用 |
|---|---|---|
| GET | `/api/todos` | 获取全部待办 |
| POST | `/api/todos` | 新增待办 |
| PUT | `/api/todos/{id}` | 更新待办 |
| DELETE | `/api/todos/{id}` | 删除待办 |

前端用 `fetch` 调用（[app.js](frontend/app.js)）：

```javascript
const API = "http://localhost:8081/api/todos";
async function load() {
  const res = await fetch(API);
  const todos = await res.json();
  // 渲染列表...
}
```

### 1.5 启动与端到端验证

- 编译：`./mvnw -q clean compile` → 全部 6 个 class 通过，代码兼容 Spring Boot 4.1.0 ✓
- 启动验证时发现 **8080 端口被本机 Jenkins 占用**（详见第 3 节），改用 8081
- 四个接口真机测试通过：GET ✓、POST ✓、PUT ✓、DELETE ✓

**踩坑（验证环节）**：POST 一度返回 400，查日志为 `JSON parse error: Invalid UTF-8 start byte 0xb2`。根因是**测试用的 curl 在这台中文 Windows 的 Git Bash 里把中文按 GBK 发出**，而 Jackson 要求 UTF-8——是测试命令的环境编码问题，**不是代码 bug**。改用 UTF-8 文件 `curl --data-binary @file` 提交后立即通过。真实浏览器 `fetch + JSON.stringify` 永远发 UTF-8，不受影响。

---

## 2. 数据库配置

### 2.1 数据库配置在哪

> 用户：数据库配置在哪里

全部在 [src/main/resources/application.properties](src/main/resources/application.properties)，分三块：
1. **数据源**（连哪个库、账号密码）：`spring.datasource.url` 等
2. **JPA / Hibernate**（建表、打印 SQL）：`spring.jpa.hibernate.ddl-auto=update` 等
3. **H2 Web 控制台**：`spring.h2.console.enabled=true`，访问 `http://localhost:8081/h2-console`

另有一份初始数据在 [data.sql](src/main/resources/data.sql)，启动时自动插入 3 条。

### 2.2 从 H2 切换到 MySQL

> 用户：我修改了这两处，是不是要把原有的注释掉啊

用户在 [application.properties](src/main/resources/application.properties) 同时保留了两份 `spring.datasource.*`（H2 + MySQL），并在 [pom.xml](pom.xml) 里让 h2 和 mysql 依赖并存。

**回答**：
- `application.properties`：**必须注释掉 H2 那组数据源**——`spring.datasource.url` 这类 key 重复时 `.properties` 按「后者覆盖前者」，靠顺序决定用哪个非常脆弱，应只留一组。
- `pom.xml`：**可以不注释**——两个驱动同在 classpath 不冲突，Spring 按 `url` 协议自动选。

**发现的问题**：用户的 MySQL URL 是 `jdbc:mysql://localhost:3306`，**缺数据库名**，会导致连上服务器但没选库、建表查询报错。修正为（自动建库 + UTF-8 + 时区）：

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/demo?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=123456
```

> 换数据库的好处：其余代码一行都不用改——这正是 JPA 的价值。

---

## 3. 排查启动报错

### 3.1 端口被占用（根因）

> 用户：（贴出 MojoExecutionException 报错结尾）

报错结尾看不出原因，自己跑复现并读完整日志，根因是：

```
Web server failed to start. Port 8081 was already in use.
```

**不是 MySQL 的问题**——日志里有 `HikariPool-1 - Shutdown`，说明数据源已成功初始化、MySQL 连得上（否则会在绑端口前就失败）。

8081 被占的来源：
- **8080**：本机 Jenkins（PID 6244）——这是当初项目改用 8081 的原因
- **8081**：之前启动留下的**孤儿 Java 进程**（`TaskStop` 杀掉了 Maven 父进程，但 `spring-boot:run` 派生的 Java 子进程在 Windows 上没被连带杀掉，成了孤儿）

清理方法：

```bash
# 查占用 PID
netstat -ano | grep ":8081.*LISTENING"
# 杀掉
taskkill //F //PID <PID>
```

清理后重启，应用成功连上 MySQL、自动建库建表 ✓。

> 教训：用户在自己终端用 `Ctrl+C` 停应用一般能干净退出，不会留孤儿进程；这里的占用是「工具强杀任务」的副作用。

### 3.2 数据重复问题

切到 MySQL（持久库）后，接口返回 **9 条数据**（3 组重复）。原因：`spring.sql.init.mode=always` 表示**每次启动都执行 data.sql**，H2 内存库每次重启本来就清空、无感；MySQL 是持久库，于是每次启动 +3 条，不断累积。

处理：
- 把 [application.properties](src/main/resources/application.properties) 的 `spring.sql.init.mode` 从 `always` 改为 **`never`**（避免重启重复插入）
- 通过 DELETE 接口清掉重复的 id 4~9，回到干净的 3 条初始数据

---

## 4. 前端 favicon 404

> 用户：有异常吗，这个 404 是怎么回事

前端 `python -m http.server 8000` 打开后，日志出现：
```
GET /            200   (index.html)
GET /style.css   200
GET /app.js      200
GET /favicon.ico 404
```

**无异常**——核心文件全部 200。`favicon.ico` 是浏览器标签页小图标，打开任何网页都会**自动**去根目录找它，项目没放该文件就 404，纯属浏览器习惯，不影响功能。

消除方法（已在 [index.html](frontend/index.html) 的 `<head>` 加一行）：

```html
<link rel="icon" href="data:,">
```

`href="data:,"` 是个空内联图标，浏览器就不再请求 `/favicon.ico`。（favicon 缓存顽固，可能需 `Ctrl+Shift+R` 强制刷新。）

另提醒：前端能打开 ≠ 数据能显示，[app.js](frontend/app.js) 一加载就 `fetch("http://localhost:8081/api/todos")`，**后端必须在 8081 跑着**，否则列表为空。

---

## 5. 代码审查与 git 初始化

### 5.1 为什么没有改动高亮

> 用户：我怎么对刚刚你修改的代码进行审查，文件没有高光显示

根因：**本项目不是 git 仓库**（`Is a git repository: false`）。VSCode 的改动高亮（装订线绿/蓝/红、Source Control 面板）依赖 git 做基线对比，没有 git 就没有「改之前 vs 现在」的参照，自然不显示。

两个方案：
- **立刻可用（无需 git）**：VSCode **时间线 Timeline**——选中文件 → 左下角 Timeline 面板 → 点旧版本与当前并排对比
- **根治（推荐）**：初始化 git

### 5.2 初始化 git

执行 `git init` + 初始提交建立基线：

```bash
git init
git add -A
git commit -m "初始化项目：Spring Boot + 原生 JS 前后端分离 Todo 示例"
```

结果：
- 提交：`e13cc78 初始化项目：Spring Boot + 原生 JS 前后端分离 Todo 示例`
- 工作区 clean（即基线已建立）
- `target/` 编译产物已被自带 `.gitignore` 忽略 ✓
- git 身份：`wangxinxin <wangxinxin@robotees.tech>`（全局已配）

以后每次改代码的审查方式：

| 方式 | 操作 |
|---|---|
| 装订线高亮 | 文件左侧自动绿（新增）/蓝（修改）/红（删除） |
| Source Control | 左侧源代码管理图标显示改动文件数 |
| 看 diff | 右键文件 → `Open Changes`，左右对比 |
| 命令行 | `git status` / `git diff` / `git diff 文件名` |

### 5.3 git 文件在本地吗

> 用户：git文件是在本地吗

**完全在本地，没有上传到任何地方**。
- `.git` 就在项目根目录（`C:\Users\爱速智\Projects\test\.git`），是本地隐藏文件夹，所有版本历史都在里面
- 刚才的 commit 离线完成，没有推送到 GitHub/Gitee 等远程
- `git remote -v` 无输出 = 没连任何云端

概念区分：**git 是本地版本管理工具；GitHub/Gitee 只是可选的「网盘」**。想备份/多机同步/分享才需要 `git push` 到远程（需自己注册建仓库授权，不会擅自执行）。删掉整个项目目录（含 `.git`）= 版本历史一并删除。

---

## 附：关键命令速查

```bash
# 后端（项目根目录）
./mvnw spring-boot:run            # Git Bash 启动
.\mvnw.cmd spring-boot:run        # PowerShell 启动

# 前端（frontend 目录）
python -m http.server 8000        # 或 npx serve -l 8000
# 然后浏览器开 http://localhost:8000

# 接口快速测试
curl http://localhost:8081/api/todos
curl -X POST http://localhost:8081/api/todos -H "Content-Type: application/json" -d '{"title":"吃饭"}'

# 数据库控制台
# 浏览器开 http://localhost:8081/h2-console（用 H2 时）

# git
git status                        # 看改了哪些文件
git diff                          # 看未提交的具体改动
git log --oneline                 # 看提交历史

# 端口排查
netstat -ano | grep ":8081.*LISTENING"   # 找占用 PID
taskkill //F //PID <PID>                 # 杀进程（Git Bash 用 //）
```

---

## 最终交付物

- 后端：[src/main/java/com/example/demo/](src/main/java/com/example/demo/) 四层结构 + 跨域配置
- 配置：[application.properties](src/main/resources/application.properties)（已切 MySQL、mode=never）
- 初始数据：[data.sql](src/main/resources/data.sql)
- 前端：[frontend/](frontend/)（index.html / app.js / style.css）
- 文档：[README.md](README.md)、本文件 [对话记录.md](对话记录.md)
- 版本基线：git commit `e13cc78`
